import type { SearchState } from '../types/domain';
export function validateAuth(name:string,email:string,password:string,signUpMode:boolean){const errors:Record<string,string>={};if(signUpMode&&!name.trim())errors.name='Name is required.';if(!email.trim())errors.email='Email is required.';if(!password.trim())errors.password='Password is required.';else if(password.length<6)errors.password='Password must be at least 6 characters.';return errors;}
export function validateSearchForm(values:SearchState){const errors:Partial<Record<keyof SearchState,string>>={};if(!values.checkIn)errors.checkIn='Check-in is required.';if(!values.checkOut)errors.checkOut='Check-out is required.';if(values.checkIn&&values.checkOut&&values.checkOut<=values.checkIn)errors.checkOut='Check-out must be later than check-in.';if(!values.guests||values.guests<1)errors.guests='Guests must be at least 1.';return errors;}
