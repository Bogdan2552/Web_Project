import { FormEvent, useState } from 'react';
import { useNavigate, Link } from 'react-router-dom';
import { useAuthState } from '../state/AuthContext';
import { validateAuth } from '../lib/validation';
export function SignUpPage(){ const navigate=useNavigate(); const {register}=useAuthState(); const [name,setName]=useState(''); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [errors,setErrors]=useState<Record<string,string>>({}); const [serverError,setServerError]=useState(''); async function handleSubmit(event:FormEvent){ event.preventDefault(); const nextErrors=validateAuth(name,email,password,true); setErrors(nextErrors); setServerError(''); if(Object.keys(nextErrors).length>0) return; try{ await register(name,email,password); navigate('/bookings'); } catch(error){ setServerError(error instanceof Error ? error.message : 'Registration failed.'); } } return <section className="auth-wrap"><div className="panel"><p className="eyebrow">Create account</p><h1>Join Paradise Stay</h1><form className="stack" onSubmit={handleSubmit}><div><label htmlFor="signup-name">Full name</label><input id="signup-name" value={name} onChange={(e)=>setName(e.target.value)}/>{errors.name&&<small className="error">{errors.name}</small>}</div><div><label htmlFor="signup-email">Email</label><input id="signup-email" value={email} onChange={(e)=>setEmail(e.target.value)}/>{errors.email&&<small className="error">{errors.email}</small>}</div><div><label htmlFor="signup-password">Password</label><input id="signup-password" type="password" value={password} onChange={(e)=>setPassword(e.target.value)}/>{errors.password&&<small className="error">{errors.password}</small>}</div>{serverError&&<small className="error">{serverError}</small>}<button type="submit" className="button-link">Create account</button><p>Already registered? <Link to="/signin">Sign in</Link>.</p></form></div></section>; }
