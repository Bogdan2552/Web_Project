import { useEffect, useState } from 'react';
import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer, PieChart, Pie, Cell } from 'recharts';
import { getDashboard } from '../../services/adminService';
import { useAuthState } from '../../state/AuthContext';
import type { DashboardPayload } from '../../types/domain';
import { InfoCard } from '../../ui/InfoCard';
export function AdminOverviewPage(){ const {user}=useAuthState(); const [data,setData]=useState<DashboardPayload|null>(null); const [message,setMessage]=useState('Loading dashboard...'); useEffect(()=>{ async function load(){ if(!user?.token) return; try{ const payload=await getDashboard(user.token); setData(payload); setMessage(''); } catch(error){ setMessage(error instanceof Error?error.message:'Failed to load dashboard.'); } } load(); },[]); return <section className="stack"><div className="panel"><p className="eyebrow">Admin</p><h1>Overview dashboard</h1><p>Summary cards and quick charts from the provided backend.</p></div>{message&&<div className="panel"><p>{message}</p></div>}{data&&<><div className="grid four"><InfoCard title="Reservations" value={data.summary.totalReservations}/><InfoCard title="Active" value={data.summary.activeReservations}/><InfoCard title="Cancelled" value={data.summary.cancelledReservations}/><InfoCard title="Users" value={data.summary.totalUsers}/></div><div className="grid two"><div className="panel chart-box"><h3>Reservations by month</h3><ResponsiveContainer width="100%" height={260}><BarChart data={data.reservationsByMonth}><XAxis dataKey="month"/><YAxis/><Tooltip/><Bar dataKey="count"/></BarChart></ResponsiveContainer></div><div className="panel chart-box"><h3>Reservations by status</h3><ResponsiveContainer width="100%" height={260}><PieChart><Pie data={data.reservationsByStatus} dataKey="count" nameKey="status" outerRadius={90}>{data.reservationsByStatus.map((entry,index)=><Cell key={entry.status+index}/>)}</Pie><Tooltip/></PieChart></ResponsiveContainer></div></div></>}</section>; }
